void main() {
  List number = [12, 156, 147, 195, 52, 13, 189, 125];
  List newnum = [];
  newnum.add(number
      .first); // by using add we are adding anything into list and by combing with the first we are restricted to add only the first one on that list
  print(number);
  print(newnum);
}
